/**
 * Class representing a collectible item in the game.
 * This class is a base class for any collectible object (such as coins, bottles, etc.).
 * It extends from `DrawableObject`, meaning it can be drawn on the canvas.
 * @extends DrawableObject
 */
class Collectible extends DrawableObject {

}
